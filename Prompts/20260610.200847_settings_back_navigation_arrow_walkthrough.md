# User Prompt

Setting screen should have a top left arrow

---

# Walkthrough - Settings Screen Back Arrow Navigation

## Changes Made

### Navigation Logic
- **Modified** [MainActivity.java](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/JSON_Validator_APK_GITHUB/app/src/main/java/com/example/jsonvalidator/MainActivity.java):
  - Added transaction back-stack inclusion for non-main fragments (like `SettingsFragment`).
  - Added back stack clearing pop mechanism when navigating to the primary `ValidationFragment`.
  - Configured `addOnBackStackChangedListener` to dynamically update Toolbar titles and Drawer active check states depending on whether the Settings screen or Validation screen is visible. When in Settings, the toolbar displays a top-left back navigation arrow.
