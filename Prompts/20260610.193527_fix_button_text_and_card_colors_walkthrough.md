# User Prompt

the buttons have black bg with black text.  fix that.
Also the file count elements too are same.
Fix those

---

# Walkthrough - Fix Button Text and Card Colors

## Changes Made

### 1. Main Layout Updates
- **Modified** [activity_main.xml](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/JSON_Validator_APK_GITHUB/app/src/main/res/layout/activity_main.xml):
  - Applied `app:cardBackgroundColor="@color/card_bg"` (white) to the main path selection card and the three summary cards (Total Files, Valid Files, Invalid Files) to guarantee they render with a solid white background.
  - Added `android:textColor="@color/on_primary"` (white) to the `btnStartValidation` button to ensure it reads cleanly over its solid black background.
  - Added `android:textColor="@color/primary"` (black) to the outlined select buttons (`btnSelectFile` and `btnSelectFolder`) to preserve the black text outline.
