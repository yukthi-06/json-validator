# User Prompt

the buttons have black bg with black text.  fix that.
Also the file count elements too are same.
Fix those

---

# Implementation Plan - Fix Button Text and Card Colors

## Proposed Changes

### Layouts

#### [MODIFY] [activity_main.xml](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/JSON_Validator_APK_GITHUB/app/src/main/res/layout/activity_main.xml)
- **Buttons styling**:
  - Explicitly set `android:textColor="@color/on_primary"` (white) on the filled button (`btnStartValidation`) to prevent it from inheriting black text on its black background.
  - Explicitly set `android:textColor="@color/primary"` (black) on the outlined buttons (`btnSelectFile` and `btnSelectFolder`).
- **Cards background styling**:
  - Add `app:cardBackgroundColor="@color/card_bg"` (white) to all cards:
    - `cardSelection`
    - Total files card
    - Valid files card
    - Invalid files card
  - This ensures they have a solid white background, preventing any default theme tints (which might be dark) from obscuring the black text.
- **Card text styling**:
  - Ensure count text and label text use appropriate contrasting text colors (black text).
